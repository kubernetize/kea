kind: pipeline
name: build

platform:
  os: linux
  arch: arm

steps:
- name: build
  image: plugins/docker
  settings:
    repo: registry.lan/krichy/kea
    registry: registry.lan
    insecure: true
    auto_tag: true
